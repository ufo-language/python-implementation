from alltypes.expr.identifier import Identifier

class CP_Ident:
    
    CONSTRAINTS = Identifier('constraints')
    DOMAIN = Identifier('domain')
    NAME = Identifier('name')
    VARIABLES = Identifier('variables')
